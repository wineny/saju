---
name: saju
description: 한국식 사주 계산기 — 명식 보기(chart) · 궁합(match) · 택일(taegil). 만세력·절기·십신·십이운성을 엔진이 계산하고 LLM은 해석만 한다. 절기를 한국 표준시로 보정해 년·월주가 틀어지지 않는다. "내 사주 봐줘", "사주 풀어줘", "궁합 봐줘", "택일 해줘", "제왕절개 날짜 골라줘", "이사 날짜", "saju" 등으로 호출.
---

# saju — 사주 명식 · 궁합 · 택일 🧭

## 제일 먼저 읽을 것

1. **사주를 절대 암산하지 마라.** 간지·절기·십신·십이운성·오행 세력은 전부 스크립트가 계산한다.
   너는 출력된 값을 **해석만** 한다. 명식을 지어내면 그게 이 스킬의 유일한 실패 모드다.
2. **생년월일시가 없으면 되묻는다.** 추측하지 마라. 시각을 모르면 날짜만으로도 돌아간다(시주는 빠진다).
3. **점수는 상대 비교값이다.** 절대적 길흉이 아니다. 채점 근거는 `references/scoring.md`.

## 실행

```bash
node bin/saju.cjs <chart|match|taegil> [옵션]
```

설치돼 있으면 `~/.claude/skills/saju/bin/saju.cjs`.

### chart — 한 사람 명식

```bash
node bin/saju.cjs chart --born 1988-03-15T07:30 --city 서울
```

명식 4기둥·오행 세력·일간 강약·십이운성·납음·십신·형충파해·절기를 찍는다.

### match — 두 사람 궁합

```bash
node bin/saju.cjs match --a 1988-03-15T07:30 --b 1986-11-02 --labels 나,상대
```

일간 관계(합·생·극·충), 일지 관계, 오행 보완, 십신을 본다. 한쪽 시각을 몰라도 된다.

### taegil — 날짜 구간 전수 계산

```bash
node bin/saju.cjs taegil --from 2028-03-06 --to 2028-03-31 \
    --dow 화목 --hours 09:00-16:00 \
    --mother 1988-03-15T07:30 --father 1986-11-02
```

| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--from` `--to` | **필수** | 후보 날짜 범위 |
| `--hours` | `08:00-18:00` | 가능한 시각대. 좁힐수록 후보가 준다 |
| `--dow` | 없음 | 가능한 요일 (`월수금`) |
| `--city` / `--lon` | 서울 | 출생지. 진태양시 보정에 쓴다 |
| `--mother` `--father` | 없음 | 주면 궁합 10점이 채점에 포함된다 |
| `--top` | 15 | 상세 출력 개수 |
| `--json` | | JSON 출력 |

## 호출 워크플로우

1. **필요한 값을 확인한다.** 생년월일시, (택일이면) 가능한 날짜 범위·시각대·요일. 없으면 **되묻는다.**
2. 스크립트를 돌린다. 궁합·택일이면 상대/부모 정보를 반드시 넣는다 — 순위가 바뀐다.
3. **해석해서 전달한다.** 점수만 나열하지 말고, 출력의 `·` 항목을 근거로 왜 그런지 설명한다.
4. 택일이면 최종 후보를 **2~3개로 좁혀** 제시한다. 하나만 찍지 마라 — 사정으로 바뀔 수 있다.

## 출력 읽는 법

- **시각 구간(`11:32~13:31`)** — 이 구간 안 아무 때나면 **같은 사주**다. 예약할 때 이 구간을 그대로 쓰면 된다.
  `09:32` 같은 어중간한 값이 정상이다 — 진태양시 −32분 보정 때문이다.
  `08:00`처럼 딱 떨어지면 `--hours` 범위에 잘린 것이다.
- **오행 수치** — 8글자 + 지장간을 세력으로 환산한 값. 0.0인 오행이 있으면 경고가 붙는다.
- **십이운성** — 일간이 일지에서 갖는 힘. 건록·제왕이면 뿌리가 튼튼한 일주다.

## 알아둘 함정

- **절기는 북경시가 아니라 한국시로 판정한다.** 엔진(lunar-javascript)이 중국제라 1시간 어긋나는 걸
  스크립트가 보정해 뒀다. 2027 입춘 = KST 02-04 **10:46** (한국천문연구원 값과 대조 완료).
  이 1시간 차이로 **년주와 월주가 통째로 바뀐다.**
- **같은 날도 시각에 따라 사주가 다르다.** 하루에 시주가 최대 6개 갈린다.
  택일에서 날짜만 고르고 시각을 안 고르면 절반을 버리는 것이다.
- **밤 시각(23시 이후)에는 쓰지 마라.** 야자시 처리를 하지 않는다.

## 정직하게 말할 것

명리는 검증된 예측 체계가 아니고 유파마다 결론이 갈린다.
사용자가 이걸 중요하게 여긴다면 **최대한 정확한 계산 위에서** 비교해 주되,
"이 날이면 잘 된다" 같은 단정은 하지 마라. **수명·중병·사고·이혼·파산은 어떤 경우에도 판정하지 않는다.**

출산 택일이라면 **의학적 안전이 점수를 이긴다**는 것을 반드시 함께 말해라.
가능한 주수 범위는 주치의가 정하고, 이 도구는 그 안에서 고르는 데만 쓴다.

## 구성

```
bin/saju.cjs           CLI (chart · match · taegil)
lib/engine.cjs         계산·채점 엔진
references/scoring.md  채점 기준 — 점수를 바꾸려면 여기부터
vendor/lunar.cjs       lunar-javascript 1.7.7 (MIT). 만세력 엔진, 수정 금지
```

Node.js만 있으면 되고 npm 설치는 필요 없다.
